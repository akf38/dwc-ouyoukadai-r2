-if favo = Favorite.find_by(user_id: current_user.id, book_id: book.id)
  = link_to "︎#{book.favorites.count}", book_favorites_path(book.id), {method: :delete, remote: true, data: {"turbolinks" => false}, class:"fas fa-heart", style: "color:red;"}
-else
  = link_to "︎#{book.favorites.count}", book_favorites_path(book.id),{method: :post, remote: true, data: {"turbolinks" => false},class: "fas fa-heart"}