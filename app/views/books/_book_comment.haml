-book.book_comments.each do |bc|
  .book-comments-box
    .book-comment
      = link_to user_path(bc.user) do 
        =attachment_image_tag bc.user, :profile_image, fallback: "no-image-icon.jpg", size: "100x100"
        %br
        = bc.user.name
    .book-comment
      = bc.content
    .book-comment
      - if bc.user == current_user
        = link_to 'Destroy', book_book_comment_path(book, bc), method: :delete, remote: true, class: "btn btn-sm btn-danger"